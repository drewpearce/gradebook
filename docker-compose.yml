services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: gradebook
      POSTGRES_PASSWORD: gradebook
      POSTGRES_DB: gradebook
    ports:
      - "5432:5432"
    volumes:
      - gradebook_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U gradebook"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  gradebook_pgdata:
